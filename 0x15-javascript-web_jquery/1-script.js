/* global $ */

// updates the text color of the <header> element to red with JQuery
// Ensure the DOM is fully loaded before running the jQuery code
$(document).ready(function () {
  $('header').css('color', '#FF0000');
});
